# love-gallery 项目交付文档

## 项目概述

一个文艺风格的**照片墙** HTML 单页应用，采用 Swiper 章节翻页 + 章节内部连续滚动的**混合布局**，每个章节以自适应网格完整展示照片。用于展示"卓"的个人照片合集，共 139 张照片，14 个页面（封面 + 12 个分类 + 结尾）。

**GitHub 仓库：** `wangjiacheng121/love_gallery_with_zhuo`
**分支：** `main`
**最新提交：** `c377911` - 章节内部滚动 + 章节间翻页混合布局

---

## 文件结构

```
love-gallery/
├── index.html           # 单页应用（所有页面 + 样式）
├── script.js            # 倒计时 + 烟花 + 音乐 + Swiper + 大图查看
├── README.md            # 原项目说明文档
├── .nojekyll            # 禁用 GitHub Pages Jekyll
├── _copy.json           # 文案配置（构建用）
├── _scan.txt            # 素材扫描报告
├── _交付文档_项目状态说明.md  # 本文件
└── assets/
    ├── audio/           # 背景音乐文件
    └── photos/          # 12个分类文件夹，共139张照片
```

---

## 已完成的工作（按时间顺序）

### 1. 图片遮挡修复（文字覆盖层居中遮挡图片的问题）
- **问题：** `.quote` 文字覆盖层 `position:absolute; top:50%; left:50%` 居中叠加在图片网格上，造成中央区域图片被完全遮挡
- **方案：** 将 `.photo-grid` 改为 `flex-wrap` 横向排布 + `align-content:flex-start`，图片从顶部开始排列
- 文字覆盖层改为半透明（`rgba(20,15,12,0.55)`），模糊降低（`blur(4px)`），让被遮挡的图片隐约可见

### 2. 第12页「光影」特殊处理（3张照片）
- **问题：** 只有3张照片，在网格中并排太小，且被文字遮挡
- **方案：** 改用 `scatter-grid` 绝对定位散落布局
  - 左上：`top:6%; left:5%`，旋转 -3°，尺寸 `clamp(140px,38vmin,320px)`
  - 右上：`top:12%; right:5%`，旋转 4°，尺寸 `clamp(120px,32vmin,280px)`
  - 底部居中：`bottom:8%; left:50%`，旋转 -2°，尺寸 `clamp(130px,35vmin,300px)`
  - 独立 `floatIn` 动画效果

### 3. 手机性能优化
- **倒计时：** 100ms → 500ms，仅值变化时更新DOM
- **烟花粒子：** 粒子数 50~90→30~60，生命周期缩短，最大火箭数 5→4
- **后台暂停：** 添加 `visibilitychange` 监听，切后台时 `cancelAnimationFrame` 暂停动画
- **CSS优化：** `will-change:transform` GPU加速，`contain:layout style` 隔离布局，`blur(10px)→blur(4px)`
- **字体回退：** 添加 `PingFang SC`、`Microsoft YaHei` 本地备选

### 4. 智能避让功能（核心功能）
- **问题：** 01、03、05、06 等页面中，文字覆盖层居中，整行图片被完全遮挡
- **方案：** 前端 JS 动态检测 `.quote` 位置，在 `.photo-grid` 中插入空白 spacer 将图片推开
- 已改用 CSS Grid 精确定位行列（见下方待解决问题 1）

### 5. 图片裁切修复（人脸/整图露出）
- **问题：** 02、03、07 等页照片被 `object-fit:cover` + 固定高度裁切，导致人脸无法露出；05 页需展示整张图片
- **方案：** `.gi img` 从固定高度 `height:clamp(80px,22vmin,200px)` 改为 `height:auto;aspect-ratio:auto`，每张照片按原始宽高比完整展示，不再裁切
- **效果：**
  - 02、03、07 页竖版人像不再截掉人脸
  - 05 页（委屈卓）等页展示整张图片
  - 第12页光影 `scatter-grid` 保留 `max-height` 限制不变

### 6. 大图查看器（新增功能）
- **需求：** 点击缩略图展示整张图片大图
- **实现：** 全屏 Lightbox 查看器
  - 点击任意缩略图弹出大图（`object-fit:contain` 完整呈现）
  - 支持点击关闭按钮、点击遮罩/图片、Esc 键关闭
  - 图片 `data-full`（预留）与 `data-caption`（可选说明）属性；默认使用 `img.src`
  - 打开时锁定页面滚动，关闭恢复
- **CSS类：** `.lightbox`、`.lightbox-img`、`.lightbox-close`、`.lightbox-caption`
- **JS函数：** `openLightbox()`、`closeLightbox()`

### 7. 混合布局：章节内部滚动 + 章节间翻页
- **需求：** 保持上一轮「章节内部连续滚动」解决堆叠，同时章节之间保留翻页切换样式
- **方案：**
  - **章节间**：保留 Swiper 全屏翻页（`direction:vertical`、分页点、滑动手势、滚轮）
  - **章节内部**：每个分类页 `.category-page` 改为 `overflow-y:auto` 内部滚动，`photo-grid` 高度 `auto` 完整露出全部照片，`scrollHeight > clientHeight` 时可在章节内上下滚动浏览
  - `.quote` 从绝对居中改为顶部标题（`order:-1` 上移），不遮挡照片
  - `adjustPhotoGrid` spacer 不再需要，已移除
  - 封面/结尾保持 `overflow:hidden`，光影 `scatter-page` 保持固定 100vh
- **效果：** 章节内连续滚动看完全部照片，滑到章节边缘触发卷动到下一章翻页
- **06页人脸裁切：** 底部三张 `.gi` 加 `face-fix`（`aspect-ratio:3/4`+`object-fit:cover`+`object-position:50% 22%`），CSS类 `.gi.face-fix img`

---

## 当前待解决的问题

### 1. 智能避让 spacer（已移除 ✅）
- 章节内部改为可滚动展示完整照片后，`.quote` 已改为顶部标题，不再遮挡照片，原智能避让 `adjustPhotoGrid` / `.gi-spacer` 逻辑已移除，不再需要

### 2. 文字覆盖层仍有遮挡
- 虽然降低了透明度和模糊，但图片仍被完全遮挡（只是隐约可见）
- 如果智能避让功能稳定，这个问题就基本解决了
- 也可以考虑：图片 alt 属性为空，且是纯装饰性，不用担心 SEO

### 3. 背景音乐文件可能缺失
- `assets/audio/` 目录下有 `music.mp3`、`bgm.mp3`、`bgmusic.mp3` 三个备选
- 如果文件不存在，JS 会静默跳过，用户无感知

---

## 技术要点（供下一任AI参考）

### 关键 CSS 类
| 类名 | 用途 | 备注 |
|------|------|------|
| `.photo-grid` | 图片网格容器 | CSS Grid，`repeat(4,1fr)`，完整展示 |
| `.gi` | 单个图片项 | 拍立得风格；`.face-fix` 人脸裁切 |
| `.quote` | 章节标题 | 顶部标题（`order:-1`） |
| `.lightbox` | 大图查看器 | 全屏遮罩 + 大图 |
| `.scatter-grid` | 光影页散落布局 | 绝对定位，`scatter-page` 固定 100vh |
| `.category-page` | 分类页容器 | `overflow-y:auto` 内部滚动 + flex column |

### 关键 JS 函数
| 函数 | 位置 | 说明 |
|------|------|------|
| `openLightbox()` / `closeLightbox()` | script.js | 大图查看器开关 |
| Swiper `slideChange` | script.js | 章节翻页 + 内部滚动复位 |
| `updateTimer()` | script.js | 倒计时更新 |
| `animate()` | script.js | 烟花粒子动画 |
| `toggleMusic()` | script.js | 背景音乐切换 |

### 动画系统
- **入场动画：** `.page.active .gi` → `dropIn`（下落弹入）
- **文字动画：** `.page.active .ch` → `rise`（模糊上浮）
- **光影页：** `.page.active .scatter-grid .gi` → `floatIn`（飘入）
- 所有动画通过 CSS `animation-delay` 错开时间

### 依赖
- **Swiper 11**（CDN）：滑动手势翻页
- **Ma Shan Zheng 字体**（Google Fonts CDN）：书法字体
- 无其他第三方依赖

---

## 后续工作建议

1. **图片懒加载优化** — 当前 139 张图片全部加载，可改为只加载当前章节和相邻章节
2. **PWA 支持** — 添加 `manifest.json` 和 Service Worker 支持离线访问
3. **GitHub Pages 部署** — 仓库已就绪，只需在 Settings → Pages 开启即可

---

## 联系方式

- **GitHub：** `wangjiacheng121`
- **邮箱：** `wjc2805745707@163.com`
- **仓库地址：** https://github.com/wangjiacheng121/love_gallery_with_zhuo